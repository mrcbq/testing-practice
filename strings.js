function stringLength(string) {
  const a = string.length;
  if ((a > 1) & (a <= 10)) {
    return a;
  } else {
    return 'String outside of the limits';
  }
}

function reverseString(string) {
  arr = string.split('')
  
}

module.exports = stringLength, reverseString;
